import { useQuasar } from 'quasar';
useQuasar();

import { describe, it, expect } from 'vitest';
import {
  VueRouterMock,
  createRouterMock,
  injectRouterMock,
} from 'vue-router-mock';

describe('', () => {
  it.todo('');
});
